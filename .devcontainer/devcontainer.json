{
  "name": "Next.js Dev Container",

  // Switch from a single image to Docker Compose so we can run
  // a Caddy reverse proxy alongside the Next.js app.
  "dockerComposeFile": "docker-compose.yml",
  "service": "app",
  "runServices": ["app", "caddy"],
  "workspaceFolder": "/workspace",

  // OrbStack automatically assigns http://caddy.test-nextjs.orb.local to the
  // Caddy container — no /etc/hosts edit or initializeCommand needed.

  // Install dependencies after the container is created
  "postCreateCommand": "npm install",

  // Start the dev server when the container starts
  "postStartCommand": "npm run dev",

  // Port 3000 is forwarded by VS Code (not bound in docker-compose) to avoid
  // conflict with OrbStack. Port 8080 is bound by the Caddy container directly
  // so VS Code doesn't need to forward it — just open the browser on it.
  "forwardPorts": [3000],
  "portsAttributes": {
    "3000": {
      "label": "Next.js (direct)",
      "onAutoForward": "silent"
    }
  },

  // macOS Docker bind mounts don't emit inotify events — polling is required
  "remoteEnv": {
    "WATCHPACK_POLLING": "true"
  },

  "customizations": {
    "vscode": {
      "extensions": [
        "dbaeumer.vscode-eslint",
        "esbenp.prettier-vscode",
        "bradlc.vscode-tailwindcss",
        "formulahendry.auto-rename-tag",
        "christian-kohler.path-intellisense"
      ],
      "settings": {
        "editor.formatOnSave": true,
        "editor.defaultFormatter": "esbenp.prettier-vscode",
        "editor.tabSize": 2
      }
    }
  }
}
